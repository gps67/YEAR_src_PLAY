#ifndef LIBR_PLUS_ZERO_MINUS_H
#define LIBR_PLUS_ZERO_MINUS_H

// YOUR PARSER must be OK with NAMESAPCE and class of same name
// OPTIMISE DIALECT to pick full expr "( %s::%s )" // () not {}

/*!
	MINUS_ZERO_PLUS is a major featire of IDX_t

	you will be able to seamlessly import %s
	as one of PEER_GROUP 
	as PEER_GROUP of %s 
		STR0 == "MINUS_ZERO_PLUS"
	
	LIBR likes "TOKEN" enough to have "SPELLING"
	CT_RT compiler is clever enough to have SPELLING = lookup("SPELLING")
	CT_RT GEN to compiler can sometime PRE_LOAD that
	CT_RT recompute_if_ZERO_or_M1
	CT_RT CHECK_NOW

	LIBR is sensitive to BITFIELD module // a PEER
	LIBR is sensitive to HEAP_ITEM_ATTR_ACTION module // a PEER
	LIBR is sensitive to DECODE_BITFIELD
	LIBR is sensitive to SIGNED_BITFIELD // a prt of BITFIELD

	PEER_LIBR ares imported at CT_RT

	LINR += import BITFIELD
	LINR += import BITFIELD _SIGNED

		create AVAR for CT action to force ti to RT
		CT_ is still clever and gives it AVAL to AVAR
		not _RT can call CT_code using CT_TOKEN_as_idx

		IDX = IDX_from_TOKEN TOKEN // this is the new stntax // 
		// MATCHABLE around here // EDIT can plant PICK_tree in CACHE
		// pick_tree // need to regrow from scratch to ensure guessable
		// recompile with modern inflection to DIALECT 
		// manually 

	SINGLE_ZERO_GUARD_BIT in steam

		above every UNS is a GUARD_BIT_ZERO
		you can have multiple zero bits
		that is a low idx in a large prealloced space for BITFIELD
		eg u7 fits in i8 and depends on GUARD_BIT_is_ZERO
		eg u7 fits in i8 // BITS 0     76543210
// ASIF	//	eg u7 fits in i8 // BITS 1     -BELOW-  // MINUS
		eg u6 fits in i8 // BITS 10	6543210
		eg u5 fits in i8 // BITS 110	 543210
		eg u4 fits in i8 // BITS 1110	  43210
		eg u3 fits in i8 // BITS 1111 0	   3210
		eg u2 fits in i8 // BITS 1111 10    210
		eg u1 fits in i8 // BITS 1111 110    10
		eg u0 fits in i8 // BITS 1111 1110    0 
		eg u0 fits in i8 // BITS 1111 1111      // NO_DATA_BITS

		eg u7 fits in i8 // BITS 1     76543210 // MINUS
		eg u0 fits in i8 // BITS 1111 1111       // LIMIT no DATA

		# that is it, the aABOVE is all # below is DIAG for PROOF
		# basically MATCH will see ABOVE one step firther
		# this is a _RT thing, as CT_ has u63 // u63_LIMIT // u63_in_i64


		eg u7 fits in i8 // BITS 1     76543210 // MINUS
		eg u6 fits in i8 // BITS 11	6543210
		eg u5 fits in i8 // BITS 111	 543210
		eg u4 fits in i8 // BITS 1111	  43210
		eg u3 fits in i8 // BITS 1111 1	   3210
		eg u2 fits in i8 // BITS 1111 11    210
		eg u1 fits in i8 // BITS 1111 111    10
		eg u0 fits in i8 // BITS 1111 1111    0 
		eg u0 fits in i8 // BITS 1111 1111       // LIMIT no DATA

		The above is a way of ALLOC using BITFIELDS
		ANother ALLOC is P0P2 where P0 < P2 simply in MINUS Range
		ANother ALLOC is P0P2 where M_z_N is extra bit or i16 M_z_N

		ONE_ZERO uses an extra bit LARGER_WORD_upto_u31 does not
		ONE_ZERO uses an extra bit LARGER_WORD_upto_u32 does not

			one derives from other, with explain of TRANSPARaNT 


			There are those that NEED
			but what they need is not available
			these tricks so not work
			a few accept that,
			and their need is so great they accept the compromise
			and just want to get on with their lives
			a many, are being duped
			these tricks do something
			these stories are glorified


			Maybe one day, the TECH will get better, 
			and the delivery may improve
			but for now, the manipulators have lied
			and your friends have told you the good news

			note the ongoing culture of undeniability
			the manipulators are bombing for fun
			and calling it sport
			because technically the ten point score index
			is under matched.
			This is not a genocide, it is a sport

			Of vourse it is not a sport (serious face)
			This is an unfortunate necessetity,
			that Madeleine Albight thinks is worth it.

			And (this message, this interview snippet)
			is mild propaganda for a real truth.

			But come judgement day,
			look at these most moral in the world beliefs,
			check out these dozen thousand fifteens and under

			And if you dont, your senator, does not
			want to be associated with those minors.
			Deep fake will be a future wave that wont go public

			So are they lying to you?
			These treatments dont WORK.
			You are better off without them.
			OH special you, OK, if you insist,
			but it is a facimile, not a star trek teleporter

*/

namespace MINUS_ZERO_PLUS {

	// USAGE import to all PEERS asif in that module
	// USAGE import to all USERS of peers asif in that ITEM
	// ITEM.field //

};
#endif
