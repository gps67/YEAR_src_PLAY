Module diary_blog { ... rest of SCRIPT ... } // silent_braces {
Module diary_blog // { ... rest of SCRIPT ... } // simpler text_zone_intutor
Module diary_blog // { ... rest of SCRIPT ... } // // silent_braces { AUTO }
# printf options data_as_ascii # all above are same obj with different { fmt }
# { fmt }
TYPE time_line _t # 
TYPE time_when _t # time_t when = WHEN # 
VAR when # auto detect match WHEN_t when #
VAR what # auto detect MATCH WHAT_t what ALIAS obj ALIAS this ALIAS SELF
TYPE WHAT_t : ALIAS subclass BASE_t Item_t ALIAS OBJ_t 
DTP pdf_page_builder
DTP FONT_PRINT _GDI
LAYOUT PAGE_like_these { ... rest of SCRIPT ... } // silent_braces { AUTO }
// { AUTO } end brace "}"
// 
// { SCRIPT }
// 
// silent_braces } // module diary_blog

// PAGE_LAYOUT // PAGES_FROM_SCROLL // api = api_SCROLL // add_item to_list
// PAGE_LAYOUT // add_item to_list // search iterate CSR item INFO
// 
# 2021-07-04 Jul Sun 22:46 mccartney "I would give up acting, for that"
# elbis iba elba
#

TEXT_MUX_FILTER # eg PFX_ eg TOKENISED eg IDX_
|
|
|

DIAG_of_PAGE # LAYOUT #
|  # PFX_MUX simply added INDENT "| " # else EOT #
|
|
|  VIEW {
|  # PFX_MUX simply added INDENT "  " # else EOT #
|  %s
|  %s # %s # CMNT_t += str # PLUS
# PLUS += feature_1234
# PLUS += feature_1234 %s == "%s" # optional syntax into data
|  }

# OUTDENT TO ZERO # then use %s%s%s pfx "_" rest_of_line
# OUTDENT push add_indent_pfx pfx rest_of_line
# OUTDENT pfx=="" pfx_sep=="" rest_of_line = line_in "%s"
# VIEW # KNOWN_VAR # VAR_POOL("VIEW")
VIEW {
	diary blog item clocked_on 
	eg draw a 7 day 2 page diary # SCROLL of ITEMS in PAGE
	eg draw a page a day
	eg draw a phrase a day
}
VIEW {
 SCROLL {
  # { TEMPLATE } # 
  # TEXT_t TEXT # TEXT_t field_name TEXT_t text_value # 
  # FIELDS date memo #
 }
}
DIAG {
 ... 7 days 3 lines
}

