	2021-11-16

	2021_11_Nov
	2021-11-16_TaskName.txt

		Task == "TaskName" 

	SIDE Task == "GSM Tesla"	// What would Nikola figure

	Solaw wind is emitted dust
	Electro Static clouds
	Magnetic Swirls

	Charged Particles in Clouds that Swirl 
	/*
		Charged Particles in Clouds that Swirl 

		creates those Named vars
		SCOPE == local to Title as A_HREF A_LINK to "Title"

			VAR Title
			VAR A_HREF
			VAR A_Link
			VAL "Title"

		META is now HYPER
		HYPER VAR Title = "Charged Particles in Clouds that Swirl" {

			VAR Charged
			VAR Particles in
			VAR Clouds that
			VAR Swirl 

			BENCH provides access to each VAR LEX ITEM
			SCRIPT 
			DTP_ITEM "Charged" // ZOOM_ABLE_TEXT


		 HYPER += SELF == HERE == THIS
		 HYPER += SELF == LOCN || SELF == ITEM || SELF == SUBLEX

		 // SUBLEX is item type one of { LOCN ITEM SUBLEX }
		 // SUBLEX is escape to wider addressed item "SUBLEX"
		 // SUBLEX "LOCN_t"
		 // SUBLEX "ITEM_t"
		 // SUBLEX "FUNC_t"
		 // SUBLEX "SUBLEX_t"
		 // SUBLEX "MORE_t" // %s "MORE" // %s PLUS //
		 // AUTO GROW LANG
		 // started off with sample value "SUBLEX" // verb adj noun etc
		 // sample value "SUBLEX = 'noun'" // verb adj noun etc
		 // sample value "SUBLEX = 'verb'" // verb adj noun etc LEX==SUBLEX
		 // sample value "{ CIDENT VALUE } // VALUE == SCRIPT + DATA
		 // CIDENT is an Item in a Module // ALIAS item module //
		 // CIDENT is the name of a local item, DATA or CODE
		 // nb SYMBOL "CIDENT_one" exists by mention
		 // so VAR TYPES must auto init // init_from_STO // %s_init //

		 Sun magnetic axis rotates every 22 years every 11 years
		 Two close frequencies = like tuning 2 strings BEATS
		 400 year cycle ANTI_BEATS DOUBLE_BEATS TWIN_BEAT
		 sparks flying off in all directions
		 but 400 year cycle has 30 year near_zero 50 year near zero PLUS

		 VAR == DOCUMENT THIS_HERE_NEAR_ZONE_zero
		 VAR == "LOCN_t" // LOCN = FOLDER GROUP // GROUP in LIST
		 Var == "A_Link" // STR0 == "EA LINK" // LINK to OUTSIDE == rhs
		 // DECODE lhs == EA rhs == LINK // PARSED DONE // GOT MATCH
		 // STR0 here == "EA_LINK" // VERB NOUN // ATTR NOUN // EA LINK
		 // LANG EA = { Get EA of stored ITEM } 
		 // AUTO_VAR EA // outside
		 // AUTO_VAR EA // inside
		 // AUTO_VAR ITEM // item
		 // KNOW BENCH has forms for things
		 // statements of the obvious, or overrides, renames
		 // USE PLAIN TEXT BENCH ENCODER
		 // AUTO VAR every word // every phrase // 

 NLP of LISP or TCL or PY or CXX

 	lines of words // comment to EOLN

	parsed ES6 PY3 Tcl lisp CXX SCRIPT

	some words are noise " of or " -in- { NLP of LISP or TCL or PY or CXX }
	// OK use "{ SP %s SP }" add outer SP too // as expect

	some words are VALUES ints hexes 

	some words are KNOWN as ...

	some words are Cap1 CAPS lower

	create CODE_POINT for each word
	create Literal fo 

 	DIALECT uses KNOWN # DIALECT SAYS # KNOWN MEANS





		 // FILTER rewrote that from COBOL to CXX SCRIPT
		}


		Charged Particles in Clouds that Swirl 
	*/


