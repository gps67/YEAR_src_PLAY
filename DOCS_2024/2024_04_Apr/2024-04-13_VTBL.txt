	2024-04-13

	2024_04_Apr
	2024-04-13_VTBL.txt

 VTBL
 	SYSTEM_CALL numbering of functions is a VTBL (or could be)
	each OBJ[idx_FUNC] .PROTOTYPE == OBJ.FUNC_PROTYOTYPE


		PROTOTYPE = TABLE_ITEM( TBL, IDX ) // SUB_SEL_("PROTOTYPE")

		INDEX idx_FUNC = lookup("ANYSTR") // or a multi step PARSED idx

		running ANYSTR through LOCAL_MINI_MACHINE acts out layout PICK

			side effects along the way
			KNOW // and when is during STEP // 

	ELF_TABLE_SLOT

