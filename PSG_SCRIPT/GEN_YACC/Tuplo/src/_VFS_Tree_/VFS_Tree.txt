2019-09-22 Sep Sun 20:23 VFS_Tree/VFS_Tree.txt

	VFS_Tree/VFS_Tree.txt

		layer = GIT checkout YEAR-MM-DD_T

		PREVIEW VERSION

			Checksum_YEAR-MM-DD_T

			checkout to $TMP_VFS/git_version_in
			checkout to $TMP_VFS/git_version_out

			WRITE_DOCS_NOW_ABOUT_THEN

				git of docs is on Bench STO
				occasional tarball
				uptodate stream of virtual tarball

				<DOC> {
				  LINK to VFS LOCN/ITEM {
				   LOCN {
				   }
				   ITEM {
				   }
				 <VIEW> {
				   LINK Locn/Item
				 }
				 <VIEW> {
				   LINK
				     ITEM
				      VFS_ROOT_
				       ROOT of expanded tarball
				        CACHE = $TMP_VFS/git_version_in
				      VFS_ROOT_
				       LAYER = show file in vfs
				       LAYER = show file in vfs
				 }
				 MATCH { LOCN ITEM }
				   "{ LOCN } { ITEM } }"
				   " { LOCN } { ITEM } } "
				 .lookup( "anystr" ); // DECODER
				 .process( " { SCRIPT } ") // ;
				}
