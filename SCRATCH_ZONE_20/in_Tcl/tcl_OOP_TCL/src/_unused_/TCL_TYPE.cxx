#include "TCL_TYPE.h"

	#if 0
	bool TCL_TYPE:: is_one_of_my_types_fn( Tcl_Obj * obj )
	{
		
		return false; // many things are // eg end of list
		return true; // everything is, filter out not ?

		// true is supposed to load a profile for SPEC
		// SPEC = SPECS[u8_idx] // return true is_UDEF UDX_t == "u8_idx"
	}
	#endif

