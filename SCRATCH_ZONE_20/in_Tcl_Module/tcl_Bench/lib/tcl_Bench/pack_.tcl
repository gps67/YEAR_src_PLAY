
proc pack_v {item} {
	# todo # find outer_packable
	# todo # parent item
	pack $item -expand 1 -fill both side top
}

proc pack_h {item} {
	# todo # find outer_packable
	# todo # parent item
	pack $item -expand 1 -fill both side left
}
