. go__fns
U1=https://www.nasm.us/pub/nasm/releasebuilds/3.01rc6/nasm-3.01rc6-xdoc.tar.bz2
U2=https://www.nasm.us/pub/nasm/releasebuilds/3.01rc6/nasm-3.01rc6.tar.bz2
if false
then
	fn_grp_url ccs \
	$U1
	echo "rename doc dir so that next IS unpacked"
fi

fn_grp_url ccs \
$U2
fn_unpack
fn_configure_pfx
# FIX # obj/config/config.h 
# FIX # src/config/config.h 
fn_log rm -f $src/config/config.h
fn_make
fn_make install
# fn_make -i
# fn_make -i install

# edit compiler.h where it includes string.h
# strncpy # needs include "bsd/string.h" #
# mkdir obj/nasm-2.16.02rc9/include/ # so pl can write something there
