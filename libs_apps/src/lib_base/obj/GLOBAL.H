#ifndef GLOBAL_H
#define GLOBAL_H

/*
	obj_ref provides some funcs for use with glib/gtk
	but avoids including it
*/
typedef unsigned int    guint;

// using <> so that gcc -MM doesnt count them 

#include <str0.h>
#include <e_print.h>

#endif
