ANIM_SCRIPT.txt

	CLOCK_EDGE FRAME_EDGE 

	PREV_ARGV

		SCRIPT EOLN

		SCRIPT TEXT 

	HERE_ARGV

		SCRIPT BOLN // default VALUE for HERE_ARGV //

	NEXT_ARGV

		ARGV == "NEXT_ARGV" // single argc += 1

		ON_SWEEP COMPILER NOTICES NEXT_ARGV is pre_parseable

		NEXT_ARGV is PARSEABLE // DECODE_USES u64_WORD_NEXT
	
 TEXT PARSE

 	via PSG and LEX and LIBR 
	have TREE_of_SGML_NODE_UDEF

		IMPORT += TREE_of_SGML_NODE_UDEF
		IMPORT += TREE_of_SGML_NODE_LIBR
		IMPORT += TREE_of_TEXT_as_MARKUP
		IMPORT += TREE_of_TEXT_as_SPEC
